# brian's GitHub Actions demonstrations
# This file is licensed under the Artistic License 2.0
#
# Each step can match against the git ref
#
# https://github.com/briandfoy/github_actions
# https://github.com/features/actions
# https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions#onpushpull_requestbranchestags
name: check-commit-message

on:
    push:
        branches:
            - '*'

jobs:
    mac-check-commit-message:
        runs-on: macos-latest
        if: "contains(github.ref, 'refs/heads/**macos')"
        steps:
            - name: Just pass
              run: true
    ubuntu-check-commit-message:
        runs-on: ubuntu-latest
        if: "contains(github.ref, 'refs/heads/**ubuntu')"
        steps:
            - name: Just pass
              run: true
    windows-check-commit-message:
        runs-on: windows-latest
        if: "contains(github.ref, 'refs/heads/**windows')"
        steps:
            - name: Just pass
              run: exit 0
